import FileUpload from '../../components/atoms/fileUpload/FileUpload';

const GifExtractor = (props) => {
  return <FileUpload style={{ height: '400px', width: '600px' }} />;
};

export default GifExtractor;
